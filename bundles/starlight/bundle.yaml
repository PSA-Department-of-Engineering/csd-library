name: STARLIGHT
version: 0.3.0
description: "Frontmatter + content-collection + page-structure invariants for a Starlight (Astro) docs site. Validates required frontmatter on every doc, ID uniqueness, reasonable date hygiene, generic page structure (intro prose, level-2 headings, type-specific Flow and Guide shapes), and base-safe linking (no root-absolute internal link targets, which 404 under a portal prefix)."
applies_to:
  - bootstrap-starlight
supported_runners:
  - vitest
maintainer: "Rafael Pires"
