name: ADR
version: 0.2.0
description: "Lifecycle invariants for Architecture Decision Records published as Starlight docs pages under src/content/docs/decisions/. Validates ADR status values, id uniqueness, that supersede links resolve, and that the decisions index's Current table still matches the records it indexes. Composes onto a Starlight site that already has the STARLIGHT bundle."
applies_to:
  - bootstrap-starlight
supported_runners:
  - vitest
maintainer: "Rafael Pires"
