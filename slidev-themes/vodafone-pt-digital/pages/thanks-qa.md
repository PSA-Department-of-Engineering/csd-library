---
layout: default
class: vfd-bare vfd-close
section: Close
---

<!-- Close: the shell's last slide, "Together we can" on the red field. `vfd-bare` drops
     the whole chrome (lockup, head, legal line, the dark stage is covered) and
     `vfd-close` has the chrome place the speechmark above this line, so the page itself
     is one element. Keep both classes. Nothing else belongs here: the ask, the contacts
     and the questions go on a content page before it, and this one holds the screen
     while people talk. -->

<div class="vfd-close-word">Together we can</div>
